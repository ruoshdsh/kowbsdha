apiVersion: v1
kind: Service
metadata:
  name: wykgwdh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wykgwdh"
    port: 8080
  selector:
    app: wykgwdh
